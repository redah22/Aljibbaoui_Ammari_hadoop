<configuration>
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="info">
        <appender-ref ref="CONSOLE" />
    </root>

    <logger name="org.apache.hadoop.util.NativeCodeLoader" level="error" />
    <logger name="org.apache.hadoop.metrics2.impl.MetricsConfig" level="error" />
    <logger name="org.apache.hadoop.metrics2.impl.MetricsSystemImpl" level="warn" />
    <logger name="org.apache.hadoop.mapreduce.JobResourceUploader" level="error" />
    <logger name="org.apache.hadoop.mapred.LocalJobRunner" level="warn" />
    <logger name="org.apache.hadoop.mapreduce.JobSubmitter" level="warn" />

</configuration>
